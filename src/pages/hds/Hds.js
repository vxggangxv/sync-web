import React from 'react';
import styled from 'styled-components';
import LogoSyncText from 'components/base/images/LogoSyncText';

export default function Hds(props) {
  // return <Styled.Jun data-component-name="Jun"></Styled.Jun>;
  return <LogoSyncText />;
}

const Styled = {
  Jun: styled.div``,
};
